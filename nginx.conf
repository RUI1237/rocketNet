server {
    listen 80;
    server_name localhost;

    # 开启 Gzip 压缩 (大幅提升加载速度)
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/json image/svg+xml;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.";

    # Brotli 压缩（比 Gzip 压缩率更高 15-20%，需要 nginx 安装 brotli 模块）
    # brotli on;
    # brotli_comp_level 6;
    # brotli_types text/plain application/javascript text/css application/json image/svg+xml;

    root /usr/share/nginx/html;
    index index.html;

    # 静态资源强缓存（Vite 打包带 hash，可以长期缓存）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # HTML 不缓存（确保更新后用户能拿到新版本）
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 🔥 核心配置：解决 React Router 刷新 404 问题
    location / {
        # 尝试找文件($uri)，找不到找目录($uri/)，再找不到就返回 index.html
        try_files $uri $uri/ /index.html;
    }

    # 可选：配置 API 反向代理 (解决生产环境跨域)
    # location /api/ {
    #     proxy_pass http://backend-server:8080/;
    #     proxy_set_header Host $host;
    # }
}
